# 📌 PIN — কাজের স্ট্যাটাস লগ (status.md)

> **নিয়ম:** প্রতিটা কাজ শেষ হওয়ার পর এই ফাইল আপডেট হবে — কী করা হলো, কখন, কেন, কীভাবে।
> সময়গুলো ঢাকা টাইম (Asia/Dhaka, UTC+6), তারিখ: ১৯ আগস্ট ২০২৬।

---

## প্রজেক্টটা কী?

**PIN — Voice Notes & Reminders।** একটা native Android অ্যাপ, যেখানে মাইক টিপে
বাংলা/বাংলিশ/ইংরেজি মিশিয়ে স্বাভাবিকভাবে কথা বললেই অ্যাপ বুঝে নেয় —
নোট লিখবে, রিমাইন্ডার দেবে, পিন করবে, নোট খুলবে বা খুঁজবে।
সব ডেটা ফোনেই থাকে। কোনো সার্ভার নেই, কোনো পেইড API নেই, খরচ শূন্য।

---

## ✅ এখন পর্যন্ত যা যা হয়েছে (টাইমলাইন)

### ১. পরিবেশ তৈরি — ভোর ৬:০৭–৬:১৫
- **কী:** বিল্ড এনভায়রনমেন্ট চেক + Android SDK ইনস্টল।
- **কেন:** এই ক্লাউড মেশিনে JDK 21 আর Gradle 8.14.3 ছিল, কিন্তু Android SDK ছিল না —
  SDK ছাড়া APK বানানো অসম্ভব।
- **কীভাবে:** Google-এর সার্ভার থেকে commandline-tools নামিয়ে
  `platform-36` (Android 16), `build-tools 36.0.0`, `platform-tools` ইনস্টল করা হয়েছে
  `/opt/android-sdk`-এ। লাইব্রেরি ভার্সনগুলোও maven.google.com থেকে লাইভ চেক করা হয়েছে
  (AGP 8.13.2, Kotlin 2.2.21, Compose BOM 2026.06.01, Room 2.8.4) — মুখস্থ ভার্সনে ভরসা করা হয়নি।

### ২. প্রজেক্ট স্ক্যাফোল্ড — ভোর ৬:১৫–৬:২০
- **কী:** Gradle প্রজেক্ট কাঠামো: `settings.gradle.kts`, version catalog
  (`gradle/libs.versions.toml`), `app/build.gradle.kts`, `AndroidManifest.xml`, `.gitignore`।
- **কেন:** পরিষ্কার, আধুনিক সিঙ্গেল-মডিউল কাঠামো — যাতে মেইনটেইন করা সহজ হয়।
- **কীভাবে:** minSdk 26, targetSdk 36, package `app.pin.voicenotes`।
  ম্যানিফেস্টে শুধু দরকারি পারমিশন: মাইক, নোটিফিকেশন, exact alarm, boot,
  আর speech service দেখার জন্য `<queries>`। ব্যাকআপ সম্পূর্ণ বন্ধ
  (`allowBackup=false`) — নোট ফোনের বাইরে যাবে না, এটাই প্রাইভেসি প্রতিশ্রুতি।

### ৩. পার্সার ইঞ্জিন (অ্যাপের মগজ) — ভোর ৬:২০–৬:৩৫
- **কী:** `domain/parser/` — ৮টা ফাইল: BanglaDigits, Vocabulary, TextNormalizer,
  PhraseMatcher, DateTimeParser, IntentParser, TitleGenerator, VoiceIntent।
- **কেন:** স্পেকের সবচেয়ে বড় দাবি — এলোমেলো কথা থেকে সঠিক কাজ বের করা,
  কিন্তু **কোনো AI/LLM ছাড়া**। তাই নিয়মভিত্তিক (deterministic) ইঞ্জিন।
- **কীভাবে:**
  - প্রতিটা শব্দকে একটা "canonical" রূপে আনা হয় (যেমন: সকালে/shokale/sokal → `shokal`,
    মনে করাইস/করিস → `korais`) — একটা ডিকশনারি টেবিলে, নতুন বানান যোগ করা এক লাইনের কাজ।
  - বাংলা সংখ্যা (১০টায়) ইংরেজি ডিজিটে কনভার্ট হয় শুধু পার্সিংয়ের জন্য —
    নোটের লেখায় ব্যবহারকারীর নিজের হরফ অক্ষত থাকে।
  - তারিখ/সময়: আজ/কাল/পরশু, সকাল/দুপুর/বিকাল/সন্ধ্যা/রাত, "10 tay", "১০:৩০",
    "9 pm", "10 minute por" — সব বোঝে। **অস্পষ্ট সময়ে কখনো অনুমান করে না** —
    "kal shokale mone korais" বললে জিজ্ঞেস করে: *কয়টায়?*
  - কমান্ড শব্দ ("লিখে রাখ", "মনে করাইস") নোট থেকে বাদ যায়, কিন্তু অর্থবহ কথা থাকে।
    আসল ট্রান্সক্রিপ্টও নোটের সাথে সংরক্ষিত থাকে।

### ৪. ডেটা লেয়ার (Room) — ভোর ৬:৩৫–৬:৪০
- **কী:** `data/` — NoteEntity, NoteDao, PinDatabase, NoteRepository।
- **কেন:** নোট আর রিমাইন্ডার ফোনে স্থায়ীভাবে রাখতে হবে; অ্যাপ বন্ধ/রিস্টার্ট হলেও থাকবে।
- **কীভাবে:** Room database (schema v1), destructive migration বন্ধ —
  ডেটা হারানো এই অ্যাপের মূল্যবোধের বিরুদ্ধে। সার্চের জন্য বাড়তি একটা
  `searchText` কলাম রাখা হয়েছে যেখানে normalized রূপ থাকে —
  তাই "sokal" লিখে খুঁজলেও "শোকাল/shokal" লেখা নোট পাওয়া যায়।

### ৫. রিমাইন্ডার ইঞ্জিন — ভোর ৬:৪০–৬:৪৫
- **কী:** `reminder/` — AlarmScheduler, ReminderReceiver, BootReceiver, NotificationHelper।
- **কেন:** ঠিক সময়ে নোটিফিকেশন+সাউন্ড+ভাইব্রেশন, আর নোটিফিকেশনে ট্যাপ করলে
  ঠিক সেই নোটটাই খুলবে।
- **কীভাবে:** AlarmManager-এর `setExactAndAllowWhileIdle`। Android 12+-এ exact alarm
  পারমিশন ইউজার বন্ধ করে দিলে ~১০ মিনিটের window-এ fallback হয় — রিমাইন্ডার হারায় না।
  ফোন রিবুট হলে BootReceiver ভবিষ্যতের রিমাইন্ডারগুলো আবার সেট করে;
  ফোন বন্ধ থাকা অবস্থায় পার হয়ে যাওয়াগুলো "Missed" মার্ক হয় — দেরি করে বাজে না।

### ৬. স্পিচ কন্ট্রোলার — ভোর ৬:৪৫–৬:৪৮
- **কী:** `speech/` — SpeechController (ইন্টারফেস) + AndroidSpeechController।
- **কেন:** ডিভাইসের নিজস্ব Android speech service ব্যবহার — জিরো খরচ।
  ইন্টারফেস আলাদা রাখায় টেস্টে নকল (fake) কন্ট্রোলার বসানো যায়।
- **কীভাবে:** ভাষা `bn-BD` চাওয়া হয়; অফলাইনে ডিভাইসে on-device recognition থাকলে
  সেটাই ব্যবহার হয়। ভয়েস না থাকলে/মাইক deny করলে টাইপিং মোড সবসময় কাজ করে —
  অ্যাপ কখনো অচল হয় না।

### ৭. UI (Jetpack Compose) — ভোর ৬:৪৮–৭:০৫
- **কী:** `ui/` — Home / Notes / Reminders / Note Detail স্ক্রিন, CaptureSheet
  (ভয়েস→কনফার্ম→ক্লারিফাই ফ্লো), ডার্ক গ্রাফাইট থিম, নিজস্ব ভেক্টর আইকন সেট, অ্যাপ আইকন।
- **কেন:** স্পেকের দাবি — প্রিমিয়াম, শান্ত, "student project" টাইপ না।
- **কীভাবে:** প্রায়-কালো surface, উষ্ণ অফ-হোয়াইট টাইপোগ্রাফি, একটাই amber accent।
  📌 পিনই অ্যাপের পরিচয় — আইকন, নোট রো, হোম সেকশনে। নিচে ৩ ট্যাব + মাঝখানে একটাই
  বড় মাইক বাটন (একটাই capture affordance)। Undo সব জায়গায় (save/pin/reminder/delete)।
  ডিলিটে সবসময় কনফার্মেশন।

### ৮. ইউনিট টেস্ট — ভোর ৭:০৫–৭:১০
- **কী:** ৬৭টা JVM টেস্ট — IntentParser (৩৪), DateTimeParser (২১), TitleGenerator (৫),
  TextNormalizer (৭)।
- **কেন:** স্পেকের section 48–50-এর প্রতিটা কমান্ড উদাহরণ + Asia/Dhaka টাইমজোনে
  মধ্যরাত-rollover, অতীত-সময় সুরক্ষা, বাংলা সংখ্যা — সব প্রমাণসহ যাচাই।
- **কীভাবে:** Fixed clock (Asia/Dhaka) দিয়ে — টেস্ট কখনো দিনের সময়ের ওপর নির্ভর করে না।

### ৯. বিল্ড + ফিক্স — ভোর ৭:১০–৭:২০
- **কী:** `./gradlew test assembleDebug`
- **ফলাফল:** প্রথম রানে **মাত্র ১টা** কম্পাইল এরর (একটা import বাদ পড়েছিল) — ফিক্স করার পর:
  - ✅ **সব ৬৭ টেস্ট পাস**
  - ✅ **APK তৈরি: `app/build/outputs/apk/debug/app-debug.apk` (~12.4 MB)**
- **আরো:** ছোট স্ক্রিনে (360dp) চিপ-রো overflow ঝুঁকি ছিল — scroll যোগ করা হয়েছে।

### ১০. Git — কমিট + পুশ — ভোর ৭:১৫–৭:২৫
- **কী:** ব্রাঞ্চ `claude/new-session-ipwpmr`-এ ২টা কমিট পুশ হয়েছে।
- **নোট:** রিপোটা একদম খালি ছিল, তাই এই ব্রাঞ্চটাই এখন default —
  আলাদা base ব্রাঞ্চ না থাকায় Pull Request খোলা সম্ভব হয়নি (PR-এর জন্য দুটো ব্রাঞ্চ লাগে)।

### ১১. মাল্টি-এজেন্ট কোড রিভিউ — ভোর ৭:২৫ থেকে চলছে ⏳
- **কী:** ৫টা আলাদা রিভিউয়ার এজেন্ট ৫টা দিক দেখছে — Android API সঠিকতা,
  পার্সার-বনাম-স্পেক, concurrency/lifecycle, UI/UX, ডেটা লেয়ার।
  প্রতিটা ফাইন্ডিং আরেকটা "সন্দেহবাদী" এজেন্ট দিয়ে যাচাই হবে — ভুয়া ফাইন্ডিং বাদ।
- **কেন:** নিজের কোড নিজে রিভিউ করলে চোখ এড়িয়ে যায় — স্বাধীন চোখ দরকার।

### ১২. বাংলা ব্যাখ্যা + এই স্ট্যাটাস ফাইল — ভোর ৭:৩০
- **কী:** ব্যবহারকারীর অনুরোধে: কোডের মূল ফাইলগুলোতে বাংলা ব্যাখ্যা ব্লক যোগ,
  আর এই `status.md` তৈরি — এখন থেকে প্রতিটা কাজের পর আপডেট হবে।

### ১৩. Race-বাগ ফিক্স — সকাল ৭:৪০
- **কী:** ক্যাপচার শিট বন্ধ করার *পরে* দেরিতে আসা স্পিচ-রেজাল্ট চুপচাপ নোট সেভ
  করে দিতে পারত — এখন শিট খোলা (শোনা/প্রসেসিং) অবস্থায় না থাকলে রেজাল্ট উপেক্ষা হয়।
- **কেন:** ব্যবহারকারী "বাতিল" বলার পরে অ্যাপের কিছু করা মানেই আস্থা ভাঙা।
- **কীভাবে:** CaptureViewModel-এর স্পিচ-স্টেট কালেক্টরে UI-অবস্থা যাচাই যোগ।

---

## 🔜 পরের ধাপ

1. ⏳ রিভিউ এজেন্টদের নিশ্চিত-হওয়া ফাইন্ডিংগুলো ফিক্স করা
2. ফিক্সের পর আবার `./gradlew test assembleDebug` — সবুজ থাকতে হবে
3. চূড়ান্ত ভিজুয়াল/পলিশ পাস
4. ফাইনাল কমিট + পুশ + চূড়ান্ত রিপোর্ট

---

## 📍 এক নজরে বর্তমান অবস্থা

| বিষয় | অবস্থা |
|---|---|
| কোর বিল্ড | ✅ পাস |
| ইউনিট টেস্ট | ✅ ৬৭/৬৭ পাস |
| ডিবাগ APK | ✅ `app/build/outputs/apk/debug/app-debug.apk` |
| Package | `app.pin.voicenotes` |
| Min / Target SDK | 26 / 36 |
| খরচ | ০ টাকা — কোনো সার্ভার/API নেই |
| রিভিউ | ⏳ চলছে |

**ইনস্টল:** `adb install -r app/build/outputs/apk/debug/app-debug.apk`
