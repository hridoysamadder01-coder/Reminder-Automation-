<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window-level theme only; all real styling lives in Compose. The dark
         window background prevents a white flash on cold start. -->
    <style name="Theme.Pin" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/pin_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
